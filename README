Lab 1 Assignment for CMPT 395

A simple CRUD application using Laravel.


How to run:
	1. Run this in the newly cloned directory:
			composer/composer.phar install

	2. Then run:
			php artisan serve --host 0.0.0.0

		to access this page outside the virtual machine